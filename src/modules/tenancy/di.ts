// Composition root del módulo `tenancy`. Esqueleto de Fase 0: aún no hay adapters que
// cablear. Se poblará por fase (Role, matriz, requirePermission, MembershipRepository,
// ProvisionWorkspaceOnUserRegistered, populate-tenant-claims). Espeja `modules/identity/di.ts`.
export {};
