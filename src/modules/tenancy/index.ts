// API pública del módulo `tenancy`: única superficie importable desde fuera (el linter
// prohíbe deep-imports). Esqueleto de Fase 0: todavía no expone nada; se irá poblando por fase
// (can, requirePermission, AuthContext, Role, errores, factory del módulo). Espeja `identity/index.ts`.
export {};
